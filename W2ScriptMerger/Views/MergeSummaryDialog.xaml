<Window x:Class="W2ScriptMerger.Views.MergeSummaryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Merge Summary"
        Height="500" Width="700"
        MinHeight="400" MinWidth="500"
        WindowStartupLocation="CenterOwner"
        Background="#1E1E1E">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#252526" Padding="15">
            <StackPanel>
                <TextBlock Text="✓ Merge Complete" FontSize="18" FontWeight="Bold" Foreground="#32CD32"/>
                <TextBlock x:Name="SummaryText" Text="All scripts merged successfully" FontSize="12" Foreground="#888888" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Content -->
        <TextBox Grid.Row="1" x:Name="ContentTextBox"
                 Background="#1E1E1E" Foreground="#D4D4D4"
                 FontFamily="Consolas" FontSize="12"
                 IsReadOnly="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 TextWrapping="NoWrap"
                 AcceptsReturn="True"
                 BorderThickness="0"
                 Margin="10"/>

        <!-- Footer -->
        <Border Grid.Row="2" Background="#252526" Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock x:Name="FolderPathText" Grid.Column="0" Foreground="#888888" VerticalAlignment="Center" 
                           TextTrimming="CharacterEllipsis" Margin="0,0,15,0"/>

                <Button Grid.Column="1" Content="📁 Open Folder" Click="OpenFolder_Click" 
                        Style="{StaticResource DarkButton}" Padding="15,8" Margin="0,0,10,0"/>
                <Button Grid.Column="2" Content="Close" Click="Close_Click" IsDefault="True"
                        Style="{StaticResource AccentButton}" Padding="15,8"/>
            </Grid>
        </Border>
    </Grid>
</Window>
