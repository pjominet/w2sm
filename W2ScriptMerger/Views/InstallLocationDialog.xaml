<Window x:Class="W2ScriptMerger.Views.InstallLocationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Choose Install Location"
        Height="220" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#2D2D2D"
        Foreground="#FFFFFF">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="{Binding Message}" TextWrapping="Wrap" Margin="0,0,0,15" FontSize="13"/>
        
        <StackPanel Grid.Row="1" Margin="0,0,0,20">
            <RadioButton x:Name="CookedPcRadio" Content="CookedPC (Game Folder)" 
                         IsChecked="{Binding IsCookedPcSelected, Mode=TwoWay}" 
                         Style="{StaticResource DarkRadioButton}" Margin="0,0,0,8" FontSize="13"/>
            <RadioButton x:Name="UserContentRadio" Content="UserContent (Documents)" 
                         IsChecked="{Binding IsUserContentSelected, Mode=TwoWay}" 
                         Style="{StaticResource DarkRadioButton}" FontSize="13"/>
        </StackPanel>
        
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Command="{Binding OkCommand}" Style="{StaticResource SuccessButton}" Padding="20,8" Margin="0,0,10,0"/>
            <Button Content="Cancel" Command="{Binding CancelCommand}" Style="{StaticResource DarkButton}" Padding="20,8"/>
        </StackPanel>
    </Grid>
</Window>
